snippet version
	\version "`system("lilypond --version | awk 'NR==1{print $3}' | tr -d '\n'")`"
snippet header
	\header {
	  title = "${1}"
	  subtitle = "${2}"
	  composer = "${3}"
	  arranger = "${3}"
	}
